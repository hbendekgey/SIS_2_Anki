## Installation
brew install poppler
pip install camelot-py
pip install pandas

## In Anki:
Create new note type "Tools > Manage Note Type" (Student): 4 fields (Front, Back, ImagePath, Id)

Need to set the card template to
{{Front}}
<br>
<img src={{ImagePath}}>

Export notes in plain text